/*  eslint linebreak-style: ["error", "windows"]    */
export const styles = theme => {
    const {
        miscellaneous: { spacing }
    } = theme;
    return {
        container: {
            marginTop: spacing * 2
        }
    };
};
